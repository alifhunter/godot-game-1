# Fonts

Drop the main UI font here as one of these filenames:

- `app_font.ttf`
- `app_font.otf`

The main menu and game UI will use the first matching file automatically. If neither file exists, the UI falls back to `OpenSans-Regular.ttf` when present. If none of those files exist, Godot keeps using the default fallback font.
